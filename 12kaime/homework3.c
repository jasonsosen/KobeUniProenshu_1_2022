//homework3.c
#include <stdio.h>

int main(void){
   int i,j;
   int A   ={{},{}} ;//2x2行列を保存するための，2次元の配列を宣言せよ．
   int v  ={,};//2次元のベクトルを保存するための，１次元の配列を宣言せよ．
   int w  ={0,0};//計算結果の２次元のベクトルを保存するための，１次元の配列を宣言せよ．
   for(){
       for(){
           w[ ] = w [ ] + A[ ][ ]*v[ ];
       }
   }
   printf("%d, %d\n", w[0], w[1]);
   return 0; 
}
