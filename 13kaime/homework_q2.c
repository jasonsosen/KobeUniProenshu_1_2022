//homework_q2.c
#include <stdio.h>

int len(c){
    int i = 0;
    
    // 入力された文字列を，文字列の最後になるまで1つずつ見ていく
    while(c[i] != 文字の最後){
        iに1を加算する．
    }
    
    return 最後にたどり着くまでに，いくつの文字を見たかを出力する ;
}

int main(void){
	char c[]="abc";
	printf("%d\n", len(c));// 3と表示されるようにしたい．
    return 0;
}
